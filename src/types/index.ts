export interface User {
    id: string;
    username: string;
    email: string;
}

export interface FibonacciInput {
    number: number;
}

export interface FibonacciSequence {
    sequence: number[];
}